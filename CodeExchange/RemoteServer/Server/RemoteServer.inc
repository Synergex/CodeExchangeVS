;; record layout for REMOTESERVERLOG:xxxxxxxx.txt (where xxxxxxxx is process id)
rbPortNumber    ,d5                     ;; local port
rbErrorText     ,a200                   ;; Error message (if port=0)

